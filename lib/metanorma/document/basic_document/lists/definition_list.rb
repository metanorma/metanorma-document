# frozen_string_literal: true

module Metanorma; module Document; module BasicDocument
  # Definition list, composed of definitions rather than list items.
  class DefinitionList < Core::Node
  end
end; end; end
