# frozen_string_literal: true

module Metanorma; module Document; module BasicDocument
  # String which is formatted according to conventions specified
  # in a named MIME type (<<rfc2046>>).
  class FormattedString < Core::Node
  end
end; end; end
