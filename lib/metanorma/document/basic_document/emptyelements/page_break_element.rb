# frozen_string_literal: true

module Metanorma; module Document; module BasicDocument
  # Page break. Only applicable in paged layouts (e.g. PDF, Word), and not
  # flow layouts (e.g. HTML).
  class PageBreakElement < Core::Node
  end
end; end; end
